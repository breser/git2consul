
var Consul = require('..');
var should = require('should');

describe('consul.agent', function () {
    var consul;

    beforeEach(function () {
        consul = new Consul();
    });

    // TODO: Implement tests for the agent endpoint

});
